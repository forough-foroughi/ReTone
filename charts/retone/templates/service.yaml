apiVersion: v1
kind: Service
metadata:
  name: {{ include "retone.fullname" . }}
  labels:
    {{- include "retone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    {{- include "retone.selectorLabels" . | nindent 4 }}
